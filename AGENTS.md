# Project Overview

This is a multi-package utility library. Each top-level directory is an independent package; there are no internal dependencies between them.

## Important Caveats

- `tracerr` — errors with stack traces

- `filesystem` — project-root locator

